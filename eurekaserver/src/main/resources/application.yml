server:
  port: 8761
  
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false 
    
management:
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include:
        - '*'    
        
spring:
  application:
    name: SERVICE-REGISTRY        